'use strict';

module.exports = function(grunt) {

    grunt.initConfig({

        /* 
        'task': {
        },
        */


    });

    // load modules
    // grunt.loadNpmTasks('...');
    
    // grunt.registerTask('default', 'description', [ 'tasks' ]);

};
